sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t liligavril/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true liligavril/react-test npm test

after_success:
  - docker build -t liligavril/multi-client ./client
  - docker build -t liligavril/multi-nginx ./nginx
  - docker build -t liligavril/multi-server ./server
  - docker build -t liligavril/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push liligavril/multi-client
  - docker push liligavril/multi-nginx
  - docker push liligavril/multi-server
  - docker push liligavril/multi-worker
